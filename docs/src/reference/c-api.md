# C API

**Not generated yet.** `foldback-sys` currently exists as an empty crate skeleton with a `cbindgen.toml` stub — real `#[no_mangle] extern "C"` functions land in Phase 3 alongside the Unity binding.

Once real, this page will link to the `cbindgen`-generated `foldback.h` (never hand-edited — see [RFC-0002](../project/rfcs/0002-c-abi-surface.md) for why generation is the source of truth) rather than duplicating its contents here, per the project's "link out, don't duplicate" rule for generated references.

Sketch of the intended surface: [Protocol Spec §3](protocol-spec.md#3-c-abi-surface-foldback-sys).
