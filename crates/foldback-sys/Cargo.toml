[package]
name = "foldback-sys"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
foldback-core = { path = "../foldback-core" }

[dev-dependencies]
tempfile = "3"
